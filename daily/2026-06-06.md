# 2026-06-06

## Todo
**Bug fixes**
- [x] `cg_tensor`: initialize `children[0]`, `children[1]`, `_backward` to NULL
- [ ] `cg_backward`: add seed `t->grad[0] = 1.0f`
- [ ] `cg_backward`: call `build_topo`, then reverse-iterate and call `_backward`
- [ ] `build_topo`: dynamic resize topo array with `realloc` when full

**Op support**
- [ ] `cg_matmul` + `backward_matmul`
- [ ] `cg_add` broadcast (for bias)
- [ ] `cg_relu` + `backward_relu`
- [ ] `cg_softmax` + `backward_softmax` (or skip, fold into loss)
- [ ] `cg_cross_entropy` + `backward_cross_entropy`

**Training infrastructure**
- [ ] `zero_grad` — clear all leaf grads before each backward
- [ ] SGD — `param->data[i] -= lr * param->grad[i]`
- [ ] distinguish leaf nodes (needs a `requires_grad` flag or `is_leaf` bool)

**Data**
- [ ] MNIST `.idx` file parser
- [ ] normalize pixels to `[0, 1]`
- [ ] mini-batch sampler

**Example**
- [ ] `mnist.c` — 784→128→ReLU→10→loss, train loop, print accuracy

## Notes & Thoughts
-
